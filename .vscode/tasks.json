{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "rm -rf Debug && mkdir Debug && cd Debug && cmake -DCMAKE_BUILD_TYPE=Debug .. && make"
        }
    ]
}